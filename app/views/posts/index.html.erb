<h1>Posts List</h1>

<ul>
	<% @posts.each do |post| %>
		<li><%= post.title %></li>
		<p><%= post.body %></p>
		<% if signed_in? %>
			<p>Posted by <%= post.user.username %></p>
		<% else %>
			<p>Posted by: <%= link_to "Sign in", signin_path %> to see the poster.</p>
		<% end %>
	<% end %>
</ul>

